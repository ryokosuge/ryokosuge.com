## Hugo Site

This directory is a Hugo documentation site using the Docsy theme via Hugo Modules.

### Commands

- `npm install` — install JS dependencies (PostCSS, Dart Sass)
- `hugo server` — start dev server
- `hugo` — build site
- `hugo mod get -u` — update Hugo modules
