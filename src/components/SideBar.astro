---
export interface Props {
  showBackHome?: boolean;
}

const { showBackHome = false } = Astro.props;
---

<div class="flex flex-col">
  {
    showBackHome && (
      <a href="/" class="text-primary-dark font-semibold text-lg">
        ← Back home
      </a>
    )
  }
</div>
