---
import { Code } from "astro/components";

import type { PostSectionCodeLang } from "../types";

export interface Props {
  lang: PostSectionCodeLang;
  code: string;
}
const { lang, code } = Astro.props;
---

<Code code={code} lang={lang} />
