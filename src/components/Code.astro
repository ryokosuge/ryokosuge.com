---
import { Code } from "astro/components";

import type { PostSectionCodeLang } from "../types";

export interface Props {
  lang?: PostSectionCodeLang;
  code: string;
  inline?: boolean;
}
const { lang, code, inline } = Astro.props;
---

<Code code={code} lang={lang} inline={inline} />
