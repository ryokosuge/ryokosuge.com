---
export interface Props {
  date: string;
}
const { date } = Astro.props;
---

<time datetime={date}>
  {
    new Date(date).toLocaleDateString("ja-JP", {
      year: "numeric",
      month: "short",
      day: "numeric",
    })
  }
</time>
