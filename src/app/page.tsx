import { Header } from "@/components/Header";

const Page = () => {
  return (
    <Header
      title="Home"
      description="メモ書き用で雑に書いてます。"
      activePath="/"
    />
  );
};

export default Page;
